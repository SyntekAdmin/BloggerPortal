var myApp = angular.module('forever21App', ['ngRoute']);

$(document).ready(function () {
    
});

document.addEventListener("DOMContentLoaded", function (event) {
    $('.loading').fadeOut('slow');
});
